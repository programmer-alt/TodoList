{
  "compilerOptions": {
     "moduleResolution": "node", // Определяет, как TypeScript должен разрешать модули. "node" означает, что TypeScript будет использовать Node.js-совместимый алгоритм разрешения модулей.
     "baseUrl": ".", // Базовый URL для разрешения не-относительных модулей. "." означает текущую директорию.
     "paths": { // Определяет пути к модулям, которые могут быть использованы для импорта. Здесь указаны пути к node_modules и src/types.
      "*": ["node_modules/*", "src/types/*"]
     },
     "jsx": "react", // Определяет, как TypeScript должен обрабатывать JSX. "react" означает, что JSX будет преобразован в вызовы React.createElement.
     "allowSyntheticDefaultImports": true, // Позволяет импортировать модули, которые не имеют экспорта по умолчанию, как если бы они имели.
     "esModuleInterop": true, // Включает совместимость с модулями ES6, позволяя импортировать модули, которые не имеют экспорта по умолчанию, как если бы они имели.
     "resolveJsonModule": true, // Позволяет импортировать JSON файлы как модули.
     "isolatedModules": true, // Указывает, что каждый файл должен быть скомпилирован независимо от других. Это необходимо для Babel и некоторых других инструментов.
     "noEmit": true, // Отключает генерацию выходных файлов.
     "strict": true, // Включает все строгие проверки типов.
     "noImplicitAny": true, // Запрещает использование типа any без явного указания.
     "strictNullChecks": true, // Включает строгую проверку на null и undefined.
     "strictFunctionTypes": true, // Включает строгую проверку типов функций.
     "strictBindCallApply": true, // Включает строгую проверку привязки функций.
     "strictPropertyInitialization": true, // Включает строгую проверку инициализации свойств.
     "noImplicitThis": true, // Запрещает использование this без явного указания типа.
     "alwaysStrict": true, // Включает строгий режим для всех файлов.
     "lib": ["dom", "dom.iterable", "esnext"], // Определяет библиотеки, которые должны быть включены в компиляцию.
     "skipLibCheck": true, // Пропускает проверку типов в объявлениях библиотек.
     "forceConsistentCasingInFileNames": true // Запрещает использование файлов с именами, которые отличаются только регистром букв.
  },
  "include": ["src"] // Определяет, какие файлы должны быть включены в компиляцию.
 }